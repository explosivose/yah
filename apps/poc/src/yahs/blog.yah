---
query:
  source: yah-blog-db
  name: blog
  type: get
  sql: |
    SELECT * FROM blog_entry
    WHERE slug = '${p.params.slug}';
  d: post
---pug
section
  nav
  h1 #{d.post.slug}
  p #{d.post.entry}
  